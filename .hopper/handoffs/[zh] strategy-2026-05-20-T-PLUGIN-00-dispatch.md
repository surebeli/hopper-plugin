# 策略 → 领导者交接：hopper-plugin 第 0 阶段峰值（2026-05-20，v2.0 取代之前的 v1.1.1 调度）

锚点：`.hopper/handoffs/strategy-2026-05-20-T-PLUGIN-00-dispatch.md::root`

> **取代**：此文件的早期内容（v1.1.1 版本发布，提交号为 `82ed578`）。原因：用户目标指令 2026-05-20 引入了 (a) 基于任务的编排，(b) 无核心组件约束，(c) 5 个供应商适配器范围（C++ 决策）。早期版本发布引用了 v1.1.1 规范；v2.0 规范要求在 Leader 弹出之前重构此版本发布。Git 历史记录中有审计跟踪（提交号为 `82ed578` → 此文件的更新）。

**发件人**：策略顾问（Claude Opus 4.7，观察者模式）

**收件人**：主领导者（GPT-5.5 xhigh，Codex CLI 位于 `F:\workspace\ai\hopper-plugin\`）

**回复**：第 0 阶段测试（T-PLUGIN-00 主机生命周期 + T-PLUGIN-00b 供应商调用）+ 第 0.5 阶段任务库引导

**参考链接**：

- `.hopper/PING.md`

- `.hopper/MANIFEST.md`

- `.hopper/AGENTS.md`（v2.0 架构 — 任务供应商偏好，而非角色绑定）

- `.hopper/queue.md`（v2 架构 — 任务类型列；已排队 18 个任务）

- `.hopper/handoffs/leader-tasklist.md`（将在 T-PLUGIN-00.5 中更新以使用任务类型）框架）

- `F:\workspace\ai\llm-hopper\docs\plans\2026-05-19-hopper-plugin-demo-spec.md`（v2.0，经过两次 Codex 审核）

- `F:\workspace\ai\llm-hopper\.hopper\USAGE-GUIDE.md` §3.4（任务类型与角色说明，新增 v2.0）

**使用的模板**：`llm-hopper/.hopper/templates/dispatch-strategy-to-leader.md` v1.0 + §12 接收者关卡。根据调度员同理心 + 接收者关卡规则。

---

## 1. 背景更改（仅供参考，请勿审核）

自 v1.1.1 版本发布以来，该文件已进行如下更改：

- **2026年5月20日用户目标指令（稍后发布）**：引入了两项架构调整——（A）基于任务的编排取代了基于角色的编排；（B）不再使用框架反应核心（利用供应商原生CLI框架）。此外，还扩展了5个供应商的适配器范围（Codex + Kimi + OpenCode + Copilot + Antigravity 或 Gemini）。

- **规范 v2.0 已编写完成**（Codex v2.0 审核前；审核目前待定）。已排队18个任务（v1.1.1 为11个），预计耗时10-12天（v1.1.1 为5天）。

- **子代理研究 2026-05-20** 确认所有 5 家供应商均可被代理调用，但存在一些限制：Kimi 的人体工程学设计最佳，OpenCode 需要 PIN 码 0.14.7，Copilot 有配额限制，Antigravity 的 OAuth 功能尚不确定（Gemini 桥接器需等到 2026-06-18 截止日期）。

- **AGENTS.md 架构 v2.0** — 供应商绑定取代了角色绑定。T-PLUGIN-00.5 任务库引导 6 种任务类型框架。

**对本次发布的影响**：T-PLUGIN-00（主机生命周期峰值）**范围不变**——仍然包含三个分支（Claude Code 插件/Codex CLI 非交互式/独立 CLI）。但 T-PLUGIN-00b 是新增的——新增四个供应商（Kimi/OpenCode/Copilot/Gemini）的供应商调用峰值。紧随其后的是 T-PLUGIN-00.5，用于引导所有下游代码实现任务所依赖的任务库。

---

2. 授权转移

### 领导人拥有完全的决定权：

- T-PLUGIN-00 / T-PLUGIN-00b / T-PLUGIN-00.5 分支执行顺序，构建器选择（非重试策略——硬性接受 #4 禁止插件内重试；如果分支在一次子进程尝试后失败，则根据 §4 #9 升级，不要静默重试）

- 是否串行或并行运行 T-PLUGIN-00 和 T-PLUGIN-00b（取决于构建器带宽）

- 写入 resolved.md 输出后，对输出进行 Critic 分发（Critic 验证 resolved 的值是具体的，而不是抽象的）

- 所有第二轮升级触发器（持续）+ P1 添加 + 第 0 阶段特定尖峰（参见 §4）

- 只要保留 3 个 (T-00) + 4 个 (T-00b) 分支的目的，即可重新调整尖峰分支的范围

- 推送 T-PLUGIN-00/00b/00.5状态行（已排队）

### 策略保留对以下事项的决策（您必须 ping）：

- 对 `F:\workspace\ai\llm-hopper\*` 文件（不同仓库）的任何修改

- 规范 v2.0 修改提案（如果 Phase 0 阶段的突发情况表明规范需要进一步修订）

- 范围扩展：请勿在 T-00 中静默添加第四个分支，或在 T-00b 中添加第五个供应商

- 供应商降级决策：如果 T-00b 发现某个供应商被阻止（例如，Antigravity OAuth 无头模式无法使用），请在将该供应商标记为仅文档供应商之前通知策略部门；策略部门将决定是否降级规范

- 是否将 hopper-plugin 仓库推送到远程仓库（策略部门授权推送）

- T-PLUGIN-00.5 任务类型框架是否需要迭代（策略部门可能希望在提交之前审核这 6 个框架）

---

## 3. 任务规范（阶段 0 + 阶段 0.5）

### T-PLUGIN-00 — 主机生命周期峰值（与 v1.1.1 版本相比范围未改变）

- 任务类型：`spec-blindspot-hunt`

- 供应商：codex-builder（通过 Codex CLI 使用 gpt-5.5-xhigh）

- 工作量：S（4 小时硬性限制）

- 规范：完整内容位于 `.hopper/handoffs/leader-tasklist.md` 的 T-PLUGIN-00 部分

- 交付物：`docs/spikes/T-PLUGIN-00-resolved.md`

- 三个方面：Claude Code 插件注册 / Codex CLI 非交互式 / 独立 CLI 基线

### T-PLUGIN-00b — 供应商调用峰值（新增 v2.0 版本）

- 任务类型：`spec-blindspot-hunt`

- 供应商：codex-builder（同一个 Builder 运行所有测试以提高上下文效率）

- 工作量：S（2 小时硬性限制，可与 T-PLUGIN-00 并行运行）

- 规范：验证 4 个供应商 CLI 的非交互式调用：

- **Kimi**：安装 `kimi-cli`（PyPI），运行 `echo "say KIMI_OK" | `kimi -p --print --afk`，文档标志确认

- **OpenCode**：安装 `opencode@0.14.7`（根据 #3213 锁定），运行 `opencode run "say OPENCODE_OK"`，文档标志确认

- **Copilot**：安装 `@github/copilot`（npm），使用具有“Copilot Requests”权限的 `GH_TOKEN` PAT 运行 `copilot -p "say COPILOT_OK"`，文档

- **Gemini**（Antigravity 桥接器）：安装 `gemini` CLI，运行 `gemini -p "say GEMINI_OK"`，文档（注意：非企业用户的弃用截止日期为 2026 年 6 月 18 日）

- `docs/spikes/T-PLUGIN-00b-vendors.md` 中每个供应商的文档：确切的调用方式、身份验证要求、观察到的怪异之处、锁定的版本

- **如果供应商无法以非交互方式运行**：标记为 D 级（仅文档适配器规范），继续处理其余供应商；不要阻止其他供应商

- **Antigravity 适配器已特别推迟**：子代理研究表明 OAuth 会阻止无头身份验证；在测试高峰期不要尝试无头安装；将 Antigravity 适配器的规范文档作为后续工作

- 验收：4 个供应商中至少有 3 个打印了预期输出；已存在 resolved.md 文件

### T-PLUGIN-00.5 — 任务库引导程序（新增 v2.0 版本）

- 任务类型：`spec-write`（编写框架属于规范型活动）

- 供应商：codex-builder（提示设计合理）

- 工作量：S（3 小时）

- 依赖：T-PLUGIN-00 + T-PLUGIN-00b 已完成（需要解析值才能完整地编写框架规范）

- 范围：在 hopper-plugin 仓库中写入 6 个 `.hopper/tasks/<type>.md` 文件。每个框架包含：

- **何时应用**：清晰的使用案例

- **验收标准**：此任务类型使用的验收标准类型（机器检查？人工验证？基于判断？）

- **Output.md 模式要求**：此任务类型特有的必填字段

- **排除项**：此任务类型不包含的内容（与相邻类型的边界）

- **供应商偏好**：此任务类型的默认供应商（与 AGENTS.md 表匹配）

- 初始 6 个框架：

- `spec-write.md`

- `code-impl.md`

- `code-review-adversarial.md`

- `code-review-acceptance.md`

- `sidecar-polish.md`

- `spec-blindspot-hunt.md`

- 验收：`.hopper/tasks/` 目录下的 6 个文件；与 queue.md 任务类型列值交叉引用；结构一致（所有 6 个布局相似）

---

4. 升级触发机制

来自 myWriteAssistant + P1 分派（持续）：

1-8：与之前的分派相同

hopper-plugin 第 0 阶段新增功能：

9. **T-PLUGIN-00 接口故障** → 停止，执行 ping 策略。可能的情况：降级跨主机声明、延长时间线、放弃插件演示

10. **T-PLUGIN-00 时间限制（>4 小时）** → 停止，写入进度摘要

11. **峰值测试揭示规范不准确** → 执行带有诊断信息的 ping 策略

新增 v2.0：

12. **T-PLUGIN-00b 供应商灾难**：如果 4 个供应商中有 ≥2 个被非交互式阻止 → 请联系策略团队。策略团队可能会将 v2.0 规范的范围从 5 个供应商降级到 3 个（至少包含 Codex、Kimi 和 OpenCode）。

13. **T-PLUGIN-00b Antigravity OAuth 证明可以实现无头模式**：这是一个令人惊喜的结果——请联系策略团队升级规范，使其包含 Antigravity 适配器实现（而不仅仅是文档）。

14. **T-PLUGIN-00.5 任务类型混淆**：如果在编写 6 个框架时发现任务类型之间的界限不明确（例如，对于一个边界任务，规范编写与代码实现之间的界限不清晰），请在继续之前联系策略团队以澄清界限。不要悄悄地制定界限决策——这些决策最终会成为长期的协议决策。

---

5. 报告频率

- **T-PLUGIN-00 已完成**：简要的 HOPPER-FEEDBACK 条目 + 根据步骤 7.5 生成 output.md

- **T-PLUGIN-00b 已完成**：同上

- **T-PLUGIN-00.5 已完成**：同上

- **所有 3 个阶段 0 任务完成后**：根据目标条件 #2（阶段完成），策略会自动对 `T-PLUGIN-00-resolved.md`、`T-PLUGIN-00b-vendors.md` 和 6 个 tasks/*.md 文件进行 `/codex` 交叉审核。此操作无需手动触发，而是由策略自动执行。只需在最终的 output.md 中发出“阶段 0 已完成，准备进行 codex 审核”的信号即可。下一个建议。

- **在 T-PLUGIN-01 分发之前（策略在 codex 通过后调用）**：策略可以使用已解析的值编写阶段 1 分发，以细化 T-PLUGIN-01..04 规范；或者委托给您。

---

## 6. 负空间（切勿使用）

- ❌ 请勿触碰 `F:\workspace\ai\llm-hopper\*`（不同的代码库）

- ❌ 请勿修改 `.hopper/PING.md`（已冻结）

- ❌ 请勿在第 0 阶段预先实现第 1 阶段及后续阶段的工作（没有解析器、没有真正的队列逻辑、没有真正的适配器代码——只有概念验证存根和文档）

- ❌ 请勿跳过编写 resolved.md / vendors.md / tasks/*.md 这 3 个交付物——它们是所有下游工作的权威来源

- ❌ 如果任何步骤失败且未获得策略确认，请勿将第 0 阶段标记为完成

- ❌ 请勿在任何代码中使用 Anthropic Agent SDK / `claude -p` / 直接使用 Anthropic SDK（规范中的硬性验收 #3）

- ❌ 请勿将 hopper-plugin 代码库推送到 GitHub 远程仓库（策略授权推送）

- ❌ 执行在 T-PLUGIN-00b 测试阶段（硬性验收 #4 — 无框架核心；测试阶段仅确认各厂商的非交互式调用，不涉及编排）期间，请勿设计重试/回退/熔断逻辑。

- ❌ 请勿在 6 个任务类型框架中使用基于角色的语言（硬性验收 #5 — 基于任务的调度；框架描述任务形状，而非代理角色）。

---

## 7. 建议的阶段光标（确认后推送到 MANIFEST.md）

```

**状态**：根据策略 2026-05-20 v2.0 分发，阶段 0 峰值 + 阶段 0.5 任务库正在进行中。领导者正在执行 T-PLUGIN-00（主机生命周期）+ T-PLUGIN-00b（供应商调用）+ T-PLUGIN-00.5（任务库引导）。

**当前光标**：构建器按第 12 节接收门中记录的顺序运行分支。T-00 上限为 4 小时，T-00b 上限为 2 小时，T-00.5 上限为 3 小时。

**下一步操作**：

1. 构建器完成 3 个分支 (T-00) + 4 个供应商检查 (T-00b) + 6 个任务类型帧 (T-00.5)。

2. 写入 docs/spikes/T-PLUGIN-00-resolved.md、docs/spikes/T-PLUGIN-00b-vendors.md 和 .hopper/tasks/*.md（共 6 个文件）。

3. 领导者标记所有 3 个任务已完成；在最终输出文件 output.md 中发出“阶段 0 完成，准备进行 codex 审核”的信号。

4. 策略自动对合并的阶段 0 输出调用 /codex 交叉审核（根据目标条件 #2）。

5. 如果 codex 审核通过：策略调度阶段 1（T-PLUGIN-01..04 管道）。

6. 如果 codex 审核失败：修改并重试。

```

---

8. 确认需求

在 `.hopper/HOPPER-FEEDBACK.md` 文件顶部添加内容（如果不存在则创建）：

```

- 2026-05-20 策略 → Leader hopper-plugin 第 0 阶段分发 v2.0 已确认；T-PLUGIN-00 + T-PLUGIN-00b + T-PLUGIN-00.5 正在启动；将生成 docs/spikes/*-resolved.md + 6 个 .hopper/tasks/*.md 作为主要交付物；策略将在我标记所有 3 个已完成后自动调用 /codex。

```

或者，如果您希望暂时跳过 HOPPER-FEEDBACK（这是一个小型早期项目），请通过聊天输出进行确认。

收到确认后执行。策略已发出，等待第 0 阶段完成信号。

---

9. 成本/时间预测

- **T-PLUGIN-00**：上限 1 美元（订阅级别，探索性）

- **T-PLUGIN-00b**：上限 0.50 美元（4 个供应商冒烟测试，每个 0.10-0.15 美元）

- **T-PLUGIN-00.5**：上限 0.50 美元（6 次帧写入，重用研究）

- **Codex 第 0 阶段审核（策略开箱即用）**：0.30-0.60 美元

- **第 0 阶段 + 第 0.5 阶段总计**：约 2.30-2.60 美元

- **时间**：4 小时 + 2 小时 + 3 小时 = 9 小时专注工作；日历 1 天（可并行处理）

---

## 10. 调度程序自检（策略在发送 v2.0 版本之前运行此检查）

5个人全部回答“是”：

1. **领导者零上下文启动？** 是 — 第 3 节引用了 leader-tasklist.md（将更新为 v2.0 任务）；v2.0 规范文档可在跨仓库获取

2. **无需 ping 即可知晓完成情况？** 是 — 第 3 节的验收要点针对每个测试阶段都具体明确；第 4 节明确规定了升级机制

3. **可区分范围内的偏差和阻塞？** 是 — 第 4 节新增了 6 个特定于测试阶段的触发器；第 6 节明确规定了留空规则

4. **输出格式已知？** 是 — 已指定 3 个交付物（resolved.md、vendors.md、tasks/*.md）

5. **支持光标的 Next？** 是 — 第 7 节明确禁止第一阶段及之后的预实现

---

11. 参考先前的派遣

- **此仓库的首次发布**（此文件取代了今天 6 小时前发布的 v1.1.1 版本；Git 中有审计跟踪）

- **同级版本传承**：myWriteAssistant strategy-2026-05-15-p0（第二轮 P0）和 strategy-2026-05-19-p1（第二轮 P1），用于协议约定参考

- **规范来源**：`F:\workspace\ai\llm-hopper\docs\plans\2026-05-19-hopper-plugin-demo-spec.md` v2.0（待 Codex 审核）

- **模板版本**：`dispatch-strategy-to-leader.md` v1.0 + §12 接收者门控

- **v2.0 起源**：用户目标指令 2026-05-20 引入了基于任务的编排 + 无 Harness Core + 5-供应商范围决策

---

## 12. 接收方执行前检查（必填——请阅读本节两遍）

> ⚠️ **停止** 执行任何工作之前，请先完成此步骤。

> ⚠️ 跳过此步骤将导致闭包失败。重述、思考、再执行是必须的。

> ⚠️ 执行前请仔细思考。交接文件中已包含所有必要信息；请勿猜测或略读。

### 必要步骤（按顺序，在第 1 节开始之前）

1. 请用您自己的语言重述 v2.0 版本发布说明，3-5 条要点，可通过聊天或提交至 `.hopper/handoffs/leader-restate-T-PLUGIN-00-v2.md` 文件。请明确包含以下内容：“v2.0 在 v1.1.1 的 T-PLUGIN-00 基础上增加了 T-PLUGIN-00b 供应商扩展和 T-PLUGIN-00.5 任务库”。

2. 请从第 6 节（留白）中找出最重要的 3 条硬性禁止规则。v2.0 特有的新增禁止规则 #8（T-00b 期间无重试/回退逻辑）和 #9（任务类型框架中无角色语言）必须包含在您的前 3 条中——这些是此发布说明旨在防止的架构反模式。

3. 请找出最有可能触发的升级触发器。建议重点关注：#12 供应商灾难（如果 4 个供应商中有 ≥2 个被阻塞，则需要降低范围）。Antigravity OAuth 的不确定性最大——但根据第 3 节，我们使用 Gemini 桥接器，因此 Antigravity 阻塞不会造成灾难性后果。

4. **说明**您的执行顺序。建议：

- 上午：T-PLUGIN-00（主机生命周期，4 小时上限）—— 不确定性最大，上限最长

- 下午：T-PLUGIN-00b（供应商调用，2 小时上限）—— 如果构建器带宽允许，可以并行执行

- 晚上：T-PLUGIN-00.5（任务库，3 小时）—— 取决于峰值输出

- 如果您选择不同的顺序，请记录下来

5. **从接收方角度镜像第 10 节调度程序自检**：任何 NO → ping 策略在执行之前。

### 严格遵守（重复以示强调）

> ⚠️ 仅执行第 3 节中指定的任务（3 个任务）。不要执行其他任何操作。不要进行第一阶段的预实现。不要“顺便把解析器也写出来”。

>
> ⚠️ 如果您发现自己需要访问 `cli/bin/`、`hosts/claude-code/.claude-plugin/`、`docs/spikes/`、`.hopper/tasks/`、`.hopper/`（队列/成本/输出/交接）之外的文件——立即停止。这是范围蔓延。触发条件 #7：Ping 策略。

>
> ⚠️ 三重强调：执行前要三思。本次测试的目的是信息收集和库引导。记录所有内容。不要做任何假设。不要进行任何发明创造。

＃＃＃ 痕迹

此门控机制是接收方对第 10 节（调度方）自检的补充。来源：`llm-hopper` `USAGE-GUIDE.md` §3.3 调度方同理心（提交 `20c2df5`，2026-05-19）。这是该模板在 0 阶段测试工作中的第二次内部验证使用。

---

大门完成后执行。策略已制定，等待第 0 阶段完成并通过规范审核后，方可启动第 1 阶段。

